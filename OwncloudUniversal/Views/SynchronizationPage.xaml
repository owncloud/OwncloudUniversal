<Page
    x:Class="OwncloudUniversal.Views.SynchronizationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OwncloudUniversal.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Template10.Controls"
    xmlns:uicontrol="using:Microsoft.Toolkit.Uwp.UI.Controls"
    DataContext="{Binding SynchronizationPageViewModel, RelativeSource={RelativeSource Self}}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:PageHeader Content="Synchronization"
                             Frame="{x:Bind Frame}"
                             Grid.Row="0"/>

        <Pivot Grid.Row="1">
            <PivotItem  Header="Status" Margin="12">
                <StackPanel>
                    <uicontrol:HeaderedTextBlock x:Name="tbStatus" Header="Status" Text="{Binding ExecutionContext.Status}" FontWeight="Bold"/>
                    <uicontrol:HeaderedTextBlock x:Name="tbFilesToSync" Header="Files to Sync" Text="{Binding ExecutionContext.FileText}" FontWeight="Bold"/>
                    <uicontrol:HeaderedTextBlock x:Name="tbFileName" Header="Current File" Text="{Binding ExecutionContext.CurrentFileName}" FontWeight="Bold"/>
                    <Button Content="Start Sync" Command="{Binding StartSyncCommand}"/>
                </StackPanel>
            </PivotItem>

            <PivotItem Header="History">
                <TextBlock Text="Coming soon.. :)"/>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
