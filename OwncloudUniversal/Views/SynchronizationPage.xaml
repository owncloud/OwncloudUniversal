<Page
    x:Class="OwncloudUniversal.Views.SynchronizationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OwncloudUniversal.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Template10.Controls"
    DataContext="{Binding SynchronizationPageViewModel, RelativeSource={RelativeSource Self}}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:PageHeader Content="Synchronization"
                             Frame="{x:Bind Frame}"
                             Grid.Row="0"/>

        <Pivot Grid.Row="1">
            <PivotItem  Header="Status" Margin="12">
                <StackPanel>
                    <TextBlock TextWrapping="Wrap" Height="20" Text="Synchronisation Status" VerticalAlignment="Top" />
                    <TextBlock x:Name="tbStatus" TextWrapping="Wrap" Text="{Binding ExecutionContext.Status}" VerticalAlignment="Top" HorizontalAlignment="Left" TextAlignment="Center" FontWeight="Bold"/>
                    <TextBlock TextWrapping="Wrap" Text="Files to sync"/>
                    <TextBlock x:Name="tbFilesToSync" TextWrapping="Wrap" Text="{Binding ExecutionContext.FileText}" FontWeight="Bold"/>
                    <TextBlock x:Name="tbFileName" TextWrapping="Wrap" Text="{Binding ExecutionContext.CurrentFileName}"/>
                    <Button Content="Start Sync" Command="{Binding StartSyncCommand}" Margin="20" HorizontalAlignment="Center"></Button>
                </StackPanel>
            </PivotItem>

            <PivotItem Header="History">

            </PivotItem>
        </Pivot>
    </Grid>
</Page>
